version: "3.9"

services:

  # ── Django / Gunicorn ─────────────────────────────────────────────────────────
  web:
    build:
      context: .
      dockerfile: Dockerfile.pi
    restart: unless-stopped
    env_file:
      - .env
    environment:
      # Force SQLite — mysqlclient is not installed in the Pi image.
      # This overrides any DB_HOST value that may be set in .env.
      DB_HOST: ""
    volumes:
      - sqlite_data:/app/db
      - media_data:/app/media
      - static_data:/app/staticfiles
    expose:
      - "8000"

  # ── Nginx ─────────────────────────────────────────────────────────────────────
  nginx:
    image: nginx:1.25-alpine
    restart: unless-stopped
    ports:
      - "${NGINX_PORT:-80}:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - static_data:/app/staticfiles:ro
      - media_data:/app/media:ro
    depends_on:
      - web

volumes:
  sqlite_data:
  media_data:
  static_data:
